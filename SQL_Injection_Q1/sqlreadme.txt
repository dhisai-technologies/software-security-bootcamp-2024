Step to use:
1.Add the Payloads File: Place the payload file in the same directory as the script.

2.Run the Script: Execute the following command in the terminal:
	python sql_injection_tester.py
3.When prompted, provide the directory path to your payloads file:
	Enter the path to the uploaded payloads file: mnt/data/payloads.txt (EXAMPLE)

4.View Results:
	The script tests each payload and prints whether the injection succeeded 
and any data retrieved from the database.