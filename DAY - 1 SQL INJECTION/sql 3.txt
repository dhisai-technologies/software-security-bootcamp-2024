search_input = "admin%' --"  # Malicious input to modify the LIKE pattern

# Vulnerable SQL query using LIKE operator
query_like = "SELECT * FROM users WHERE username LIKE '%" + search_input + "%'"

# Another example of SQL injection in LIKE query
search_input2 = "admin' OR '1'='1"  # Malicious input to bypass authentication
query_like2 = "SELECT * FROM users WHERE username LIKE '%" + search_input2 + "%'"

# Safe parameterized query using LIKE (No vulnerability)
query_safe = "SELECT * FROM users WHERE username LIKE ?"


